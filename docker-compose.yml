version: "3.9"
services:
  demo-app:
    build: .
    container_name: demo-app
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 10s
      retries: 5
